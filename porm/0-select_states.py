#!/usr/bin/python3
from MySQLdb import MySQLdb
class states:
    my __init_(self,mysql username, mysql password,database name):
        self.username = mysql username
        self.password = mysql password
        self.dababase = database name
    (f"id,states"zipode = 3306)
